<!-- Share configuration related to this particular Share module extension, 
	such as: - Doclib action config - Form config - Aspect and Type config Note. 
	Any configuration related to the whole Share.war webapp (i.e. not specific 
	to this share extension) should be placed in the environment specific config: 
	alfresco-allinone/share/src/main/resources/alfresco/web-extension/share-config-custom.xml 
	file -->
<alfresco-config>

	<visible>
		<!--Add our aspect -->
		<aspect name="acme:recordData" />
	</visible>

	<!-- ACME Aspect fields -->
	<config condition="acme:recordData" evaluator="aspect">
		<forms>
			<form>
				<field-visibility>
					<show id="acme:recordSource" set="rm-metadata-acme-recordData" />
					<show id="acme:recordContributor" set="rm-metadata-acme-recordData" />
				</field-visibility>
				<appearance>
					<set appearance="panel" id="rm-metadata-acme-recordData" />
					<field id="acme:recordSource" set="rm-metadata-acme-recordData" />
					<field id="acme:recordContributor" set="rm-metadata-acme-recordData" />
				</appearance>
			</form>
		</forms>
	</config>
</alfresco-config>
